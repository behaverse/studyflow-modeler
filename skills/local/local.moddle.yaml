# yaml-language-server: $schema=https://unpkg.com/moddle/resources/schema/moddle.json

# No elements of its own: this runtime adds itself to the study's `runtime` choices.
name: Local runtime
prefix: local
description: >-
  On the researcher's machine.
uri: https://w3id.org/studyflow/local
icon: iconify tabler--hexagon-letter-l
order: 200
xml:
  tagAlias: lowerCase
version: 26.0910

types: []

enumerations:
  - extends: studyflow:RuntimeEnum
    literalValues:
      - name: Local
        value: local
        description: On the researcher's machine.
