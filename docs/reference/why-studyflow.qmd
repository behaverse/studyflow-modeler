---
title: Why Studyflow
description: Your tools compute, Studyflow coordinates -- the case for making coordination a first-class artifact
order: 1
---

## Computation and coordination

Gelernter and Carriero argued in 1992[^gc92] that a complete programming model has two parts: a *computation model* — how one step does its work — and a *coordination model* — how the steps fit together: their order, their dependencies, what each one hands the next.

[^gc92]: David Gelernter and Nicholas Carriero, "Coordination Languages and their Significance", *Communications of the ACM* 35(2), 1992.

A study has both parts; only one is well served. Computation has good tools: jsPsych and PsychoPy present trials, Python and R fit models, a Unity scene renders a task, an LLM call scores a response. Coordination has none. It lives in a preregistration, a Methods section, a slide, and the glue code — several copies of one thing, in places nothing keeps in step.

**Your tools compute. Studyflow coordinates.**

## Drift is the concrete problem

A scientific study touches many people and systems. The researcher designs it. The ethics reviewer approves it. The participant runs it. The data pipeline ingests it. The model trains on it. The reviewer reads it in a manuscript. The replicator tries to rebuild it.

Each of those audiences gets a different artifact today: a protocol PDF, a registration form, an experiment script, a preprocessing notebook, a model card, a manuscript figure, a CONSORT diagram. Reorder the blocks in the script and nothing updates the registration. The artifacts drift apart, and by the end no single thing describes the study as it was actually run.

## One file, several projections

Studyflow externalizes that coordination. One `.studyflow.yaml` file:

- Reads as a study protocol for a colleague, a reviewer, or an ethics board.
- Generates a publication-quality figure that carries its own source (SVG or PNG).
- Specifies the data pipeline that the Python runner executes to produce analysis outputs.
- Drives the browser runner that administers the study to participants.
- Documents the model and decision points behind a deployed predictor.

Each is a projection of one file, not a copy. The diagram in the paper *is* the protocol the participants ran *is* the pipeline that produced the figures: there is one source, so the artifacts cannot hold divergent copies of a value.

## Two audiences, one language

Studyflow serves two primary audiences explicitly:

- **Experimental researchers** model studies: recruitment, consent, randomization, task blocks, questionnaires, dropouts, data collection.
- **Pipeline and model builders** model data flow: ingestion, preprocessing, transformation, model training, evaluation, deployment, monitoring.

Both audiences use the same elements. A study that ends in a model training pipeline is one diagram, not two stitched together. A pipeline that triggers a retraining when fairness degrades looks like a study with a feedback loop. The boundary between "doing science" and "operating a model" stops being a documentation cliff.

## Why BPMN as the base

BPMN 2.0, standardized as ISO/IEC 19510, is a mature process notation with an existing ecosystem of editors, validators, and engines. It already covers events, activities, gateways (the branch points), sub-processes, data objects, and choreography between participants. Building on BPMN means Studyflow gets all of that for free, plus a shape vocabulary that engineers and analysts outside science already recognize.

It also sets the ambition: as BPMN is the shared process standard for business, Studyflow aims to be that for studies — readable by every audience, executable by tools, owned by no vendor.

Studyflow extends BPMN where research needs it: domain-specific activity types (`CognitiveTask`, `Questionnaire`, `Rest`), research-specific data structures (`Dataset`, `Table`, `Schema`, `DataCatalog`), data operations as task markers (`Map`, `Filter`, `Reduce`), and a `Study` container at the top level. See [Studyflow vs BPMN](studyflow-vs-bpmn.qmd) for the precise delta.

## Trust by construction

Trusting science requires rigor and reproducibility. As we increasingly rely on machines to facilitate research, trust in code and data is becoming critical too -- especially at scale, with large models in the loop.

A studyflow is human-readable (a researcher can review it), machine-readable (the browser and Python runners execute it), and reusable (parts of one study can seed another). That makes reproducibility a property of the artifact itself, not a separate documentation effort maintained alongside the work.
