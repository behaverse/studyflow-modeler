---
title: "Developers"
description: "The programs that read studyflow files, how to build and run them, what a run produces, and how to extend the vocabulary."
---

| Program | Does | Lives in |
|:--|:--|:--|
| the modeler | authoring: edit, validate, import and export a studyflow | [`packages/modeler`](https://github.com/behaverse/studyflow-modeler/tree/main/packages/modeler) |
| the browser runner | runs the participant-facing steps, served at `./run/` | [`packages/runner`](https://github.com/behaverse/studyflow-modeler/tree/main/packages/runner) |
| the Python runner | runs the data-facing steps — the ones bound to software | [`packages/runner-py`](https://github.com/behaverse/studyflow-modeler/tree/main/packages/runner-py) |
| the command line | `studyflow convert · validate · run · render · info` | [`packages/cli`](https://github.com/behaverse/studyflow-modeler/tree/main/packages/cli) |
| the core | the object model they all share: YAML ⇄ BPMN, element access | [`packages/core`](https://github.com/behaverse/studyflow-modeler/tree/main/packages/core) |
| the schemas | the vocabulary every program loads at boot | [`assets/schemas`](https://github.com/behaverse/studyflow-modeler/tree/main/assets/schemas) |


Install the command line with Homebrew:

```bash
brew tap behaverse/studyflow https://github.com/behaverse/studyflow-modeler
brew install studyflow
# uv tool install studyflow-runner
```


The command line is extensible: `studyflow <name>` runs `studyflow-<name>` from your PATH if `<name>` is not a built-in, handing the rest of the arguments through.

## How to build and run

```bash
git clone https://github.com/behaverse/studyflow-modeler
cd studyflow-modeler
npm run dev          # the modeler and the browser runner
npm run test:unit    # unit tests only
npm test             # all tests, e2e included
```


## One study, three carriers

The same study is stored as YAML (canonical format), as BPMN 2.0 XML (for interoperability; this is what other BPMN tools read), and as a PNG or SVG with the diagram embedded inside (e.g., for visualizing or sending a study to someone). Conversion between the three is lossless. For example:

```bash
studyflow validate study.studyflow.yaml --strict
studyflow convert study.studyflow.yaml study.bpmn
studyflow convert study.studyflow.yaml study.studyflow.png
studyflow info study.studyflow.yaml --json
```

## Running a study

![One run, from the saved file to its records: validation gates it, the study's `runtime` attribute picks the executor, and the Python runner leaves a repository behind.](assets/img/diagrams/run-lifecycle.svg){#fig-lifecycle fig-alt="A flowchart. An author saves a studyflow, validation gates it, and the run goes to the browser runner when runtime is browser or to the Python runner when runtime is local. The browser runner posts sessions and events, opt-in. The Python runner archives the plan into a run repository with one commit per checkpoint; per-element records let untouched steps skip on a partial re-run, and an invalidated record branches the repository at that step's parent."}

There are currently two executors, and they split the file by what a step *does*: participant-facing steps run in the browser, steps bound to software run in Python, and each executor steps over the other's half. `studyflow run` reads the `runtime` attribute on the study and dispatches — `browser` studies belong at the published `./run/` URL, `local` ones go to the Python runner on your machine. An eval is the same walk with a model in the participant's seat — set `agentType` to `bot` on the task — so the same two executors run it; no separate harness code.

::: {layout-ncol=4}
![Consent, from the start event](assets/img/runner/consent.png){#fig-run-consent fig-alt="A browser screen presenting a consent document with an accept button."}

![An instruction step](assets/img/runner/instruction.png){#fig-run-instruction fig-alt="A browser screen showing instruction text and a continue button."}

![A questionnaire step](assets/img/runner/questionnaire.png){#fig-run-questionnaire fig-alt="A browser screen presenting questionnaire items with response options."}

![Completion, from the end event](assets/img/runner/completion.png){#fig-run-completion fig-alt="A browser screen thanking the participant and showing a completion code."}
:::

Gateways decide branches by rules:

| Gateway | Rule |
|:--|:--|
| An exclusive gateway, and `cognitive:EligibilityGateway` | the first outgoing edge whose condition holds, otherwise the edge marked `default` |
| `cognitive:RandomGateway` | a seeded uniform draw over the outgoing edges |
| A parallel gateway | every outgoing edge; the matching join waits for all of them |
| An inclusive gateway | every outgoing edge whose condition holds |
| `agentic:Router` | a label chosen by a model |

- An executor that cannot evaluate a condition stops, rather than silently taking the other branc
- A condition naming a value not in its scope reports an exception, not `false`.
- A `seed` makes every gateway draw replay exact.

| Element | | Browser runner | Python runner |
|:--|:--|:--|:--|
| `bpmn:StartEvent`, `bpmn:EndEvent` | consent link shape; completion-code coherence | consent screen; completion screen, code, redirect | reached and recorded, no screen |
| `cognitive:Instruction`, `cognitive:Questionnaire` | empty content or instrument blocks the run | renders; writes the answer into scope | — |
| `cognitive:BehaverseTask` | checked against the assessment build's manifest | runs the assessment in the page | — |
| `cognitive:CognitiveTask`, `cognitive:Rest`, plain `bpmn:Task` | — | generic screen, and a way forward | — |
| Any activity carrying `implementation` | reference grammar; literal arguments must be a mapping | shows the call it names | imports and calls it, binding the drawn data associations |
| `bpmn:SubProcess` | — | descends; opens its own scope | walks one level in; no separate scope |
| `bpmn:ExclusiveGateway`, `cognitive:EligibilityGateway` | — | conditions, then `default` | the same rule, in Python |
| `cognitive:RandomGateway` | warns where allocation attributes cannot be honored | seeded uniform draw | treated as exclusive |
| `studyflow:Dataset`, `studyflow:Table`, `studyflow:Timeseries`, `studyflow:Schema`, `studyflow:EventMarker` | — | — | loaded and saved wherever they carry a `uri` |
| `studyflow:Parameters` | reports overridden, undeclared, and unbound names | bound before the walk | — |
| Timers, and multi-instance markers | — | not executed | not executed |

: What each executor does with each family. A blank is a step it passes over. {#tbl-support}


## What a run leaves behind

![The record lives in the document itself: a trail on the root, a record per element, and — for Python runs — a repository whose commits restate the same attributes.](assets/img/diagrams/provenance-model.svg){#fig-provenance-model fig-alt="Flowchart. The studyflow document carries a flat provenance trail on its root element and per-element records in each element's extension elements. The modeler appends to the trail and voids records; the Python runner appends to the trail, stamps records on the archived copy, and commits every checkpoint into a run repository. Each commit restates the trail's attributes as trailers and keeps that checkpoint's step records in its body. No sidecar record file is written."}

Before it runs, a studyflow is a plan; as it runs, the record of what actually executed accumulates in the same file (following W3C PROV's split between prospective and retrospective provenance). So "did the study deviate from the plan?" is answered by diffing one file against its earlier self. Provenance entries are stored in the same file as the studyflow.

| Attribute | Holds | | Action | Records |
|:--|:--|:-:|:--|:--|
| `action` | what happened, in one word | | `created`, `modified` | the file was authored, or edited and exported again |
| `when` | an ISO 8601 timestamp; ties break by position | | `imported` | an artifact was staged in from outside |
| `who` | a name, an email, or an ORCID | | `executed` | the document ran, or one element did |
| `with` | the tool that acted, as `name/version` | | `invalidated` | a marker voiding a named execution record |
| `what` | element ids, or the `when` of the entry referred to | | `reused` | a step was skipped and an earlier record trusted |
| `run`, `seed` | the run's identifier, and its root seed | | | |

The records also drive partial re-runs: a step is skipped when its record still stands, nothing it reads was re-made this run, and its outputs still exist. Invalidating a record cascades to everything downstream of the outputs it re-makes.

## Extending the notation

![The seven shipped sets and how they reach the loaded catalog: four always load, three switch on in the modeler's settings.](assets/img/diagrams/schema-map.svg){#fig-schema-map fig-alt="A flowchart. Four core sets — studyflow, prov, functional, cognitive — always load into the catalog. Three optional sets — ml, agentic, eeg — load only when enabled in Settings, Extensions. The ml set declares no types of its own and roots its twelve presets at the functional set. The catalog compiles every enabled set into the palette, the inspector tabs, and the namespaces the XML may use."}

New vocabulary can be provided as an extension. Each new type extends an existing one and adds attributes, so files using it stay well formed and still open in generic BPMN tools. Before declaring a type, check whether you need one at all.

Be clear about what a declaration buys:

| Declaring a type gives it | It does not give it |
|:--|:--|
| a place in the palette, a shape, and an icon | a screen or a call in either executor |
| fields in the inspector | pre-flight checks over the values an author types |
| storage and retrieval through every file format | a way for a run to do anything at that step |

Making a new type *executable* means telling an executor about it, either execute an element or just render it. As executors need to support the execution, @tbl-support is the current list of what runs.

A set's namespace carries its format version.

## Contributing

[`CONTRIBUTING.md`](https://github.com/behaverse/studyflow-modeler/blob/main/CONTRIBUTING.md).