# Generated by packages/cli/scripts/package.mjs — do not edit by hand.
class Studyflow < Formula
  desc "Command-line tool for studyflow diagrams"
  homepage "https://github.com/morteza/studyflow-modeler"
  license "MIT"
  # No `version`: Homebrew scans 26.0615 out of the asset names, and audit calls a second copy redundant.

  on_macos do
    on_arm do
      url "https://github.com/morteza/studyflow-modeler/releases/download/v26.0615/studyflow-26.0615-darwin-arm64.tar.gz"
      sha256 "4292dd3f43775150f11c58713d0c8c0fe109206556a9923c3d09e45fa7a13623"
    end
    on_intel do
      url "https://github.com/morteza/studyflow-modeler/releases/download/v26.0615/studyflow-26.0615-darwin-x64.tar.gz"
      sha256 "97f767276cfda86ae43c0bfef489b375a9560e574bd342bfc7320108ea4467ad"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/morteza/studyflow-modeler/releases/download/v26.0615/studyflow-26.0615-linux-arm64.tar.gz"
      sha256 "a16dbeee6da06e543c630890bf3b949015cac8c4d8f316efc33fd09222c6d561"
    end
    on_intel do
      url "https://github.com/morteza/studyflow-modeler/releases/download/v26.0615/studyflow-26.0615-linux-x64.tar.gz"
      sha256 "a1071ca8a0ad3dcd821ce8de4713e09f5320dae415f0bb36c089b5b75adebb69"
    end
  end

  def install
    bin.install "studyflow"
    # The fallback runner for `studyflow run --runtime local`; found next to the binary, and driven by uv.
    libexec.install "studyflow-prov.py"
  end

  def caveats
    <<~EOS
      convert, validate and info are self-contained: nothing else to install.

      `studyflow run --runtime local` executes a study, which needs Python. Either
      install the runner as a companion, the way git finds git-lfs:
        uv tool install studyflow-runner   # puts studyflow-run on your PATH
      or install uv alone, and the copy in this keg is used instead:
        brew install uv
    EOS
  end

  test do
    (testpath/"minimal.studyflow").write <<~YAML
      id: brew_test
      definitions:
        targetNamespace: http://bpmn.io/schema/bpmn
      Brew_Test:
        type: bpmn:Process
        name: Brew test
        flowElements:
          Start:
            type: bpmn:StartEvent
          Done:
            type: bpmn:EndEvent
    YAML
    assert_match version.to_s, shell_output("#{bin}/studyflow --version")
    assert_match "OK", shell_output("#{bin}/studyflow validate #{testpath}/minimal.studyflow")
    assert_match "Brew test", shell_output("#{bin}/studyflow info #{testpath}/minimal.studyflow")
  end
end
